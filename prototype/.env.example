# Optional. If unset, the app uses mock extraction (demo mode).
# Never commit a real key. Copy this file to .env.local for local use.
AI_API_KEY=

# Optional. Vision-capable OpenAI model used when AI_API_KEY is set.
AI_MODEL=gpt-4o-mini
